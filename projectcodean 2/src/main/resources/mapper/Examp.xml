<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codeanapp.project.mapper.ExampMapper">

    <insert id="insertExamp" parameterType="com.codeanapp.project.model.Examp">
        INSERT INTO examp (idexamp, stackid, soal, keterangan)
        VALUES (#{idexamp}, #{stackid}, #{soal}, #{keterangan})
    </insert>

    <select id="getAllExamp" resultType="com.codeanapp.project.model.Examp">
        SELECT idexamp, stackid, soal, keterangan
        FROM examp
    </select>

    <update id="updateExamp" parameterType="com.codeanapp.project.dto.ExampUpdateDto">
        UPDATE examp
        <set>
            <if test="oldidexamp !=null">
                idexamp = #{oldidexamp},
            </if>
            <if test="stackid != null">
                stackid = #{stackid},
            </if>
            <if test="soal">
                soal = #{soal},
            </if>
            <if test="keterangan">
                keterangan = #{keterangan}
            </if>
        </set>
            WHERE idexamp = #{oldidexamp}
    </update>

    <delete id="deleteExamp" parameterType="com.codeanapp.project.model.Examp">
        DELETE FROM examp
        WHERE idexamp = #{idexamp}
    </delete>
</mapper>