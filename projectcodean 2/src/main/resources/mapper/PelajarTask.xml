<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codeanapp.project.mapper.PelajarTaskMapper">

    <insert id="insertPelajarTask" parameterType="com.codeanapp.project.model.PelajarTask">
        INSERT INTO pelajartask (idpelajartask, taskid, pelajarid, nilai)
        VALUES (#{idpelajartask}, #{taskid}, #{pelajarid}, #{nilai})
    </insert>

    <select id="getAllPelajarTask" resultType="com.codeanapp.project.model.PelajarTask">
        SELECT idpelajartask, taskid, pelajarid, nilai
        FROM pelajartask
        ORDER BY
        <choose>
            <when test="sort == 'idAsc'"> idpelajartask ASC</when>
        </choose>
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countPelajarTask" resultType="int">
        SELECT COUNT (*) FROM pelajartask
    </select>

    <delete id="deletePelajarTask" parameterType="String">
        DELETE FROM pelajartask
        WHERE idpelajartask = #{idpelajartask}
    </delete>

</mapper>
